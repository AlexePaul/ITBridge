export default defineAppConfig({
  ui: {
    // Both roles point at the same gold: the classical system is mono — it
    // carries a second accent ramp only so both sets resolve.
    colors: {
      primary: "accent",
      secondary: "accent",
      neutral: "neutral",
    },
  },
});
