// The contract lives in packages/types. The usual thin bridge, so pages import from `~/types/...`.
export type {
  MailTemplateSummary,
  MailTemplateDetail,
  MailTemplateFields,
  MailTemplateRendered,
  UpdateMailTemplateDto,
  PreviewMailTemplateDto,
} from "@itbridge/types";
